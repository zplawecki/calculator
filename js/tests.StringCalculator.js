function test_printing(a, b) {
    if ((a.length == 1) || (b.length == 1) || (a.length == 2) || (b.length == 2)) {
        return "ok";
    }
    else {
         return "fail, string is too short or too long";
    }
}

function add(a, b) {
    
}